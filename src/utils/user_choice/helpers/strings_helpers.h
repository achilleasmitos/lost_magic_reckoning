#pragma once

#include <string>

namespace user_choice_helpers
{
/**
 * @brief Returns the lower-cased version of its input.
 *
 * @param input
 * @return std::string
 */
std::string ToLowerCase(std::string const& input);
} // namespace user_choice_helpers
